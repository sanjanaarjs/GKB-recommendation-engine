"use client";

import PdpWrapper from "@/components/custom/pdp/pdpWrapper";

export default function PdpPage() {
    return (
        <div>
            <PdpWrapper productData={null} currency={null} lensData={null} />
        </div>
    );
}
